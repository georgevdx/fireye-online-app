Fire-S RC 1.2.1G - Filter Stability Hotfix

Upload:
- app.js
- styles.css
- README-RC-1-2-1G.txt

Fixes:
- Mission Control status dropdown filters now use one stable lifecycle matcher.
- Sort order uses full timestamps where available, so newest/oldest is consistent.
- More Filters starts collapsed every time the Projects/Home page opens.
- More Filters only opens when clicked and keeps the label stable.

No new modules added.
